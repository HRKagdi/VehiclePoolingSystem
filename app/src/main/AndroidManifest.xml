<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vehiclepooling">

    <uses-permission android:name="android.permission.INTERNET  " />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/app_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/splashscreen"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Navigation_Bar" />
        <activity android:name=".Ride_History" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SourceDestinationSeatSelection" />
        <activity android:name=".MapNavigation" />
        <activity android:name=".AvailableRide" />
        <activity android:name=".Select_vehicle" />
        <activity android:name=".Ride" />
        <activity android:name=".Vr" />
        <activity android:name=".Choice_Activity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Login_Activity" />
    </application>

</manifest>